"""Decision Engine — KG-based diagnostic decision layer.

Sits on top of the existing pipeline (retrieval → mapping → fusion)
and computes calibrated confidence, mode determination, reasoning
chains, and explanations.
"""
